# MAGMA 当前事实更正（2026-05-26）

- 本文后续出现的 `MiniLM-L6-v2` / `384 维` 是早期落地状态，不是当前运行态。
- 当前 embedding 模型：本地 `BAAI/bge-small-zh-v1.5`，维度 512。
- 当前 SQLite/FAISS 向量已重建为 512 维，`data/faiss_meta.json` 记录 model=bge-small-zh-v1.5。
- LLM 后端和 embedding 模型是两套东西：慢路径 LLM 后端是 DeepSeek V3 via OpenRouter；向量召回 embedding 是本地 bge-small-zh-v1.5。

---

# MAGMA 多图代理记忆系统 — 项目起源与架构设计

**整理日期**：2026-05-26
**用途**：给 AI/新成员快速了解"为什么做 MAGMA、怎么来的、原始架构是什么"

---

## 1. 背景：OpenClaw Agent 没有跨会话记忆

### 1.1 问题的根源

我运营的 OpenClaw 电商多 Agent 系统（zhuli 总调度 / yunying 运营部 / jishu 技术部）有一个根本缺陷：**Agent 对话结束后就"失忆"了**。

具体表现：
- 下次问"上次为什么部署失败"——Agent 不知道
- 问"防晒衣主图做到哪了"——Agent 不知道
- 问"上次讨论的方案选了哪个"——Agent 不知道

每次对话都是从零开始，之前的经验、教训、决策全部丢失。

### 1.2 当时已有的记忆手段

| 方案 | 能做什么 | 问题 |
|------|---------|------|
| **MEMORY.md / memory/*.md** | 人工维护的结构化笔记 | 需要人手动写，AI 只能被动读 |
| **WorkBuddy memory_search** | FTS5 关键词搜索 | 不智能，搜"防晒衣"搜不到"防晒服" |
| **GBrain**（已退役） | OpenAI embedding API 语义搜索 | 联网付费，Gemini Flash 国内 403，中文好但贵且依赖网络 |

没有一个方案能同时满足：**跨会话持久 + 语义检索 + 本地部署 + 不花钱**。

---

## 2. 触发点：发现 MAGMA 论文

### 2.1 初次接触

在调试 OpenClaw deliveryContext 路由 Bug 的同一次对话中，我随口问了"MAGMA 是什么"。当时了解到：

- **论文**：MAGMA: Multi-Graph based Agentic Memory Architecture（ACL 2026, arxiv:2601.03236）
- **核心思想**：用四张正交图（时间/语义/因果/实体）分别承载不同类型的关系，检索时按查询意图分配权重
- **关键洞察**："为什么"靠因果图，"什么时候"靠时间图，"谁/什么"靠实体图，"类似的"靠语义图

### 2.2 论文调研

我让 AI 去查了论文原文（arxiv:2601.03236），核心架构要点：

```
MAGMA 论文架构：
├── 数据写入
│   ├── 快路径（Synaptic）→ 同步写入事件 + 向量 + 时间边，<50ms
│   └── 慢路径（Consolidation）→ 异步 LLM 推断因果关系 + 实体关系
│
├── 四正交图存储
│   ├── Temporal Graph → 严格有序时间链（NEXT 边）
│   ├── Semantic Graph → 语义相似边（cos > θ）
│   ├── Causal Graph → 因果边（LLM 推断 + confidence）
│   └── Entity Graph → 事件↔抽象实体关联
│
├── 检索：Beam Search
│   ├── Step 1: 意图分类（Why/When/Entity/General）
│   ├── Step 2: RRF 混合锚点搜索（向量 + 关键词）
│   └── Step 3: 策略引导的 Beam Traversal（按意图权重选边）
│
└── 自适应权重映射（论文核心贡献）
    | 意图    | w_temporal | w_causal | w_entity | w_semantic |
    |---------|-----------|----------|----------|------------|
    | why     | 0.5       | 5.0      | 2.5      | 1.0        |
    | when    | 4.0       | 1.0      | 1.0      | 1.0        |
    | entity  | 1.0       | 1.0      | 6.0      | 2.0        |
    | general | 1.0       | 1.0      | 1.0      | 3.0        |
```

### 2.3 GitHub 仓库调研

我让 AI 去查了官方仓库 https://github.com/FredJiang0324/MAGMA（MIT 协议），发现：

- 仓库有完整的论文复现代码
- 但它是学术研究代码，不是生产就绪的
- 需要基于论文思路重新实现，适配我们的场景

### 2.4 视频分析

我还提供了一段中文讲解视频（~10.5 分钟，`C:\Users\Administrator\Desktop\证明图\mmexport1779676635716.mp4`），让 AI 分析视频内容以辅助理解论文的运作机制。视频直观演示了：

- MAGMA 的多图协作流程
- Beam Search 的遍历过程
- 意图分类如何影响检索路径
- 快/慢双路径的写入和巩固过程

这段视频对理解论文的实际运行效果很有帮助，比纯看文字论文直观得多。

### 2.5 对比其他方案

在决定做 MAGMA 之前，我还问了 GBrIN（另一个记忆方案），搜索后没有找到确定信息，被排除了。

最终对比：

| 方案 | 问题 |
|------|------|
| 继续用 GBrain | 联网付费，Gemini Flash 国内 403，不可控 |
| 纯向量数据库（ChromaDB） | 只有语义相似，没有时序和因果 |
| GBrIN | 找不到可靠信息，放弃 |
| **MAGMA 四图架构** | 时间+语义+因果+实体，正交解耦，最完整，本地可跑 |

---

## 3. 核心目标

**一句话：让 Agent 能记住跨会话的重要信息，并能按语义关联检索。**

拆开来看：

| 目标 | 说明 |
|------|------|
| **跨会话持久化** | 对话结束不丢记忆，下次能回忆 |
| **语义检索** | 搜"防晒衣"能关联到"防晒服主图设计" |
| **因果追溯** | 能回答"为什么上次部署失败" |
| **时间线感知** | 能回答"上次什么时候做的" |
| **本地部署** | VM 上跑，不依赖外部 API，不花钱 |
| **Agent 分权** | zhuli 全权，yunying 记运营，jishu 记技术 |

---

## 4. 原始架构设计

基于论文架构，结合 VM 8GB 内存约束做了全套技术选型妥协。

### 4.1 整体架构图

```
                       OpenClaw Agents
                      (zhuli/yunying/jishu)
                            |
                +-----------+-----------+
                |                       |
          WorkBuddy Skill          OpenClaw Plugin
          (SKILL.md + CLI)      (definePluginEntry)
                |                       |
                +-----------+-----------+
                            |
                +-----------+-----------+
                |    FastAPI Service     |
                |    (localhost:8901)     |
                +-----------+-----------+
                            |
              +-----------------+-----------------+
              |         MAGMA Core Engine         |
              |  +----------------------------+  |
              |  |   Policy Router (意图分类)    |  |
              |  +----------------------------+  |
              |              |                   |
              |  +-----------+-----------+       |
              |  |  Adaptive Hybrid      |       |
              |  |  Retrieval (Beam)     |       |
              |  +-----------+-----------+       |
              |              |                   |
              |  +-----+ +-----+ +-----+ +----+ |
              |  |SemG | |TmpG | |CauG | |EntG| |
              |  +--+--+ +--+--+ +--+--+ +-+--+ |
              |     |       |       |       |    |
              |  +--v-------v-------v-------v--+ |
              |  |     Graph Layer (SQLite)    | |
              |  +-----------------------------+ |
              |              |                   |
              |  +-----------+-----------+       |
              |  |  Vector Store (FAISS)  |       |
              |  |  + Sentence-Transformers|       |
              |  +-----------------------+       |
              +-----------------------------------+
                            |
              +-----------------+-----------------+
              |      Memory Ingestion Pipeline      |
              |  +----------+  +------------------+ |
              |  |Fast Path |  | Slow Path        | |
              |  |(Synaptic)|  |(Consolidation)   | |
              |  | 同步写入  |  | 异步 LLM 推断    | |
              |  +----------+  +------------------+ |
              +-------------------------------------+
                            |
              +-----------------+-----------------+
              |    Data Sources (初始导入)          |
              |  MEMORY.md, YYYY-MM-DD.md          |
              |  + 飞书消息流 (实时)                |
              +-------------------------------------+
```

### 4.2 技术选型决策

| 决策 | 选了 | 没选 | 理由 |
|------|------|------|------|
| 图存储 | SQLite | Neo4j | VM 8GB 跑不动 JVM |
| 向量索引 | FAISS IndexFlatIP | ChromaDB | 更轻量，与 torch 兼容 |
| Embedding | MiniLM-L6-v2 | text2vec-base-chinese | 90MB vs 400MB，先跑通再换 |
| 意图分类 | 正则规则 | LLM | 零延迟，80% 覆盖够用 |
| 慢路径 LLM | 规则兜底 + 可插拔 | 固定 API | 先不依赖外部，后续可接 |
| 中文分词 | jieba | pkuseg | 更轻量，电商领域够用 |

### 4.3 数据模型（SQLite Schema）

**四正交图边表**：

| 图 | 边表 | 特性 |
|---|---|---|
| 时间图 | edges_temporal | 严格有序对，不可变链 |
| 语义图 | edges_semantic | 无向边，cos(v_i, v_j) > θ_sim |
| 因果图 | edges_causal | 有向边，LLM 推断 + confidence + rationale |
| 实体图 | edges_entity | 事件 ↔ 抽象实体（person/product/project/tool/concept） |

### 4.4 检索流程

```
Query → Intent Classifier → Policy Weights → RRF Anchor Search
  → Beam Traversal (max 5 hops) → Topological Sort → Context Serialization
  → OpenClaw Agent Response
```

### 4.5 双流写入

- **快路径（同步, <50ms）**：文本分段 → 提取事件 → 写 events 表 → 计算向量写 FAISS → 建 temporal 边 → 推入 consolidation_queue
- **慢路径（异步, 后台）**：从队列取 event → 获取 2 跳邻域 → LLM/规则推断因果 + 实体 → 更新因果图 + 实体图

---

## 5. 实施阶段规划（原始）

| Phase | 内容 | 预估时间 |
|-------|------|---------|
| Phase 0 | VM 环境准备 | 0.5天 |
| Phase 1 | 基础骨架（数据可写入读取） | 2-3天 |
| Phase 2 | 记忆摄入管线（Markdown + 实时消息导入） | 2天 |
| Phase 3 | 检索引擎（意图分类 + Beam Search） | 3天 |
| Phase 4 | API + Skill 集成 | 2天 |
| Phase 5 | 优化 + 生产化 | 2天 |

---

## 6. 约束条件（决定技术选型的硬限制）

| 约束 | 影响 |
|------|------|
| VM 只有 8GB 内存 | 排除 Neo4j、大模型本地部署 |
| VM 只有 C 盘（D 是光驱） | 所有数据存 C 盘 |
| 国内网络环境 | HuggingFace 必须用 hf-mirror.com 镜像 |
| 不想花钱 | Embedding 走本地，LLM 走免费/极低价 API |
| Windows 10 x64 | torch DLL 兼容性问题 |

---

## 7. 实际落地情况

- **Phase 1-5**：宿主机开发完成，168 测试全绿（8 个 FAISS 环境限制失败）
- **Phase A**：部署到 VM，jishu 执行安装，872 entities / 875 vectors / 127 events
- **Phase B**：MCP 集成到 OpenClaw，5 工具已加载，全链路验证通过
- **LLM 后端**：OpenRouter + DeepSeek V3（Gemini Flash 国内 403，换成了 dsv3）
- **已知短板**：Embedding 用的是英文模型 MiniLM-L6-v2，中文语义理解差

### 7.1 与论文的差异

| 论文设计 | 实际实现 | 原因 |
|---------|---------|------|
| 4 正交图边表 | 合并为 3 表（temporal/semantic/causal）+ entities | SQLite 递归 CTE 多表 UNION 不稳定，改 Python BFS |
| LLM 慢路径 | 先规则兜底，后接 OpenRouter dsv3 | VM 跑不动本地大模型 |
| BM25 + 向量混合检索 | FAISS 向量 + 关键词正则 | jieba BM25 未完整实现 |
| 原生 OpenClaw Plugin | MCP 协议集成 | OpenClaw 的 MCP 生态更成熟 |

---

## 8. 关键参考

| 资料 | 位置 |
|------|------|
| MAGMA 论文 | arxiv:2601.03236 |
| 官方仓库 | https://github.com/FredJiang0324/MAGMA (MIT) |
| 视频讲解 | `C:\Users\Administrator\Desktop\证明图\mmexport1779676635716.mp4`（~10.5min） |
| 原始实施计划 | `C:\Users\Administrator\.workbuddy\plans\electric-beacon-tesla.md` |
| 源代码 | 宿主机 `D:\CIaw\work\magma\` |
| VM 部署 | VM `C:\openclaw-magma\` |
| Skill 注册 | `C:\Users\Administrator\.workbuddy\skills\magma-memory\SKILL.md` |
| 工作日志 | `d:\CIaw\work\.workbuddy\memory\2026-05-25.md` |

---

> 本文档记录了 MAGMA 记忆系统从"发现问题 → 论文调研 → GitHub 仓库分析 → 视频理解 → 架构设计 → 技术选型"的完整起源过程。
